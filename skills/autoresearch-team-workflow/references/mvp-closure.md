# MVP closure

`MVP-CLOSED-MINIMAL-E2E` is a负责人验收 item, not a member task package.

Mark it `accepted` only when:

1. A and B lane tasks required by the registry are accepted;
2. I0 shared contracts/application integration is complete;
3. I1 pipeline orchestration is complete;
4. I2 runs the minimal Evaluation Section scenario on merged main;
5. I3 closes the Project-to-Act, task ledger, artifact, and evidence records;
6. I4 verifies the minimal manuscript/local delivery output;
7. unknown, blocked, needs_material, unexecuted results, and rollback boundaries remain explicit;
8. no blocking review finding remains.

MVP closure does not imply production scale, multiple paper types, all venues, complete UI, performance baselines, or every future optimization.
