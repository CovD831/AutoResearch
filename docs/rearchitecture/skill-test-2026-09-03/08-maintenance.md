# Maintenance and Canonical Ownership

本目录拥有本次 skill-test 的设计材料；源码和项目账本仍是 current authority。manifest 是机器索引，Markdown 是 rationale，JSON review report 是 reviewer 原始报告权威，review ledger 是消费权威。

任何边界、写入权、部署范围或恢复语义改变，都必须新开 review round；只改排版不触发新 round。
