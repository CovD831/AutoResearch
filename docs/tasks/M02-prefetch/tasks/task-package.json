{
  "task_id": "M02-PREFETCH",
  "phase": "R007",
  "owner_lane": "L-02",
  "status": "handoff",
  "base_ref": "8dd8780",
  "branch": "feat/r007-l02-prefetch",
  "depends_on": ["K1 RunManifest (L-05) for run_id wiring only -- blocking only that one field"],
  "merge_after": ["K1 RunManifest frozen (optional: only needed to populate PrefetchRecord.run_id)"],
  "allowed_paths": [
    "src/autoresearch/evidence_prefetch.py",
    "src/autoresearch/invalidation.py",
    "tests/test_m02_prefetch.py",
    "tests/test_m02_invalidation.py",
    "docs/tasks/M02-prefetch/",
    ".ai-team/tasks/M02-PREFETCH.md"
  ],
  "forbidden_paths": [
    "src/autoresearch/knowledge.py",
    "src/autoresearch/storage.py",
    "src/autoresearch/contracts.py",
    "src/autoresearch/evidence.py",
    "src/autoresearch/gates.py",
    "src/autoresearch/application.py",
    "src/autoresearch/api.py",
    "src/autoresearch/cli.py",
    ".ai-team/TASK.md",
    ".project-to-act/"
  ],
  "deliverables": [
    "K7 PrefetchRecord schema + K7-2 fail-closed validator + K7-3 bundle foreign key",
    "K7-1 structural compensation: a single L2+ entry that writes the prefetch record before the action, with the action signature requiring that record",
    "K8 InvalidationPropagation schema + K8-1 permanent-block mapping table + K8-3 non-empty claim validator",
    "K8-2 two-hop propagation (evidence -> claims -> gates) over the graph_edge records",
    "reuse of B5 updated-by[] retraction verdict (no re-implementation)",
    "offline tests covering N-1..N-4 plus structural guards",
    "task-local ledger and L3"
  ],
  "acceptance_commands": [
    "PYTHONPATH=src python -m pytest tests/test_m02_*.py -q -o addopts=\"\" -W error",
    "PYTHONPATH=src python -m pytest -q -o addopts=\"\" -W error",
    "python -m ruff check src tests",
    "python -m compileall -q src",
    "python scripts/check_pr_contract.py --base main",
    "node .ai-team/check.mjs --base main"
  ],
  "next_package": "L-05 run_manifest.py (K1) -- only to populate PrefetchRecord.run_id"
}
